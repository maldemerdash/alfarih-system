# نسخة v175 - بطاقة مسير الشهر تعرض صافي الصرف النهائي

تم البناء على نسخة v174 مع إبقاء بنية التشغيل نفسها:

- `index.html`
- `style.css`
- `app.js`
- ملفات Supabase وملفات الشرح المرافقة

أهم ما تم تعديله في v175:

- تعديل بطاقة الشاشة الرئيسية من `رواتب هذا الشهر` إلى `مسير هذا الشهر`.
- لم تعد البطاقة تجمع الرواتب الأساسية أو إجمالي الراتب الخام.
- أصبحت البطاقة تعرض قيمة المسير النهائي المحول للموظفين بعد البدلات والإضافات والخصومات والتأمينات والغياب والسلفيات.
- إذا كان مسير الشهر الحالي محفوظًا يتم قراءة الرقم من المسير المحفوظ.
- إذا لم يكن المسير محفوظًا يتم حسابه مباشرة بنفس منطق شاشة الرواتب.
- يعتمد حساب بطاقة الشاشة الرئيسية على الشهر الحالي فعليًا، وليس آخر شهر تم اختياره داخل شاشة الرواتب.
- إضافة إجمالي `transfer` إلى إجماليات المسيرات الجديدة لدعم رقم الصرف النهائي صراحة.
- تحديث روابط الكاش إلى `v=175`.

ما تم تعديله في v174:

تم البناء على نسخة v173 مع إبقاء بنية التشغيل نفسها:

- `index.html`
- `style.css`
- `app.js`
- ملفات Supabase وملفات الشرح المرافقة

أهم ما تم تعديله في v174:

- تطبيق منطقية علامة العين ونافذة معاينة المرفقات على تذكرة السفر وتأشيرة الخروج والعودة.
- إضافة عمود `المرفقات` في جدول السفر داخل صفحة `الإجازات والسفر`، وفيه زر عين مستقل للتذكرة وزر عين مستقل للتأشيرة.
- تحويل أزرار تحميل التذكرة والتأشيرة داخل سجل الموظف `سجل الإجازات والسفر` إلى أزرار معاينة، مع إبقاء التحميل داخل نافذة المعاينة.
- إضافة طبقة حماية تحول أي زر تحميل قديم للتذكرة أو التأشيرة إلى زر معاينة حتى لو ظهر من كاش واجهة قديم.
- عدم جلب رابط أو بيانات ملف التذكرة/التأشيرة عند فتح الجدول؛ يتم الجلب فقط عند الضغط على علامة العين مع الاستفادة من كاش المرفقات الموجود.
- تحديث صفحة الإجازات والسفر بعد حفظ المرفقات أو البيانات بدون تحميل المرفقات الثقيلة مسبقًا.
- تحديث روابط الكاش إلى `v=174`.

ما تم تعديله في v173:

تم البناء على نسخة v172 مع إبقاء بنية التشغيل نفسها:

- `index.html`
- `style.css`
- `app.js`
- ملفات Supabase وملفات الشرح المرافقة

أهم ما تم تعديله في v173:

- إزالة التدرجات نهائيًا من شاشة تسجيل الدخول عند وجود خلفية محفوظة؛ الخلفية تظهر كصورة فقط.
- جعل شاشة `جاري تجهيز النظام` بيضاء فقط، ولا تعرض خلفية تسجيل الدخول ولا أي تدرج لوني.
- تحميل صورة خلفية الدخول مسبقًا قبل إظهار شاشة الدخول حتى لا تظهر الخلفية القديمة ثم تتبدل.
- إضافة كاش خفيف لروابط وبيانات المرفقات لتقليل ثقل جلب المرفقات المتكرر.
- إزالة مراقبة الخصائص والتمرير الزمني المتكرر لأزرار المرفقات بعد أن تم تثبيت منطق زر العين في الطبقات القديمة.
- تحديث روابط الكاش إلى `v=173`.

ما تم تعديله في v172:

- توحيد سلوك خانات المرفقات في الموقع: عند اختيار ملف يتحول زر المرفق إلى حالة `جاري الرفع` تفاعلية، ثم يعود إلى شكل المرفق المحفوظ بعد اكتمال الرفع.
- إضافة زر معاينة بعلامة العين للمرفقات، يفتح نافذة منبثقة داخلية بهوية الموقع بدل الاعتماد على فتح المتصفح المباشر.
- نافذة المعاينة تدعم الصور وملفات PDF، وتعرض رسالة مناسبة للأنواع التي لا يمكن معاينتها داخل المتصفح مع بقاء زر التحميل.
- تحويل معاينة العنوان الوطني ومرفقات الموظف/الوثائق إلى نفس منطق المعاينة الموحد قدر الإمكان.
- تحديث الواجهة بعد عمليات الحفظ بشكل خفيف وسريع، بإعادة رسم الصفحة النشطة فقط وتحسين أزرار المرفقات الجديدة دون تشغيل حفظ إضافي غير لازم.
- تجهيز خلفية شاشة الدخول قبل إظهار البوابة عند وجود خلفية محفوظة، لمنع ظهور الخلفية الافتراضية ثم تبدلها.
- تحديث روابط الكاش إلى `v=172`.

ما تم تعديله في v171:

- تحويل رسالة حذف السلفة في الرواتب من `confirm` الخاص بالمتصفح إلى نافذة منبثقة داخلية من هوية الموقع.
- إضافة ستار تحميل قصير بهوية النظام يمنع ظهور الشاشة القديمة أو رسائل الصلاحيات المؤقتة أثناء تحميل الصفحة.
- إظهار الواجهة بعد اكتمال تحميل المستخدم، البيانات، الصلاحيات، والرسم النهائي.
- تحديث روابط الكاش إلى `v=171`.

ما تم تعديله في v170:

- استبدال رسالة المتصفح الافتراضية عند حذف التنبيه الخاص بنافذة منبثقة داخلية من نفس هوية الموقع.
- إضافة نافذة تأكيد بعنوان `حذف التنبيه الخاص؟` مع زر إلغاء وزر حذف واضح.
- الحفاظ على منطق الحذف الحالي: ينتقل التنبيه إلى جدول `التنبيهات المحذوفة` مع تاريخ الحذف واسم المستخدم.
- تحديث روابط الكاش إلى `v=170`.

ما تم تعديله في v169:

- إصلاح تداخل قائمة `التنبيهات الخاصة` مع قائمة `الإشعارات`.
- عند فتح الإشعارات العامة يتم إغلاق قائمة التنبيهات الخاصة تلقائيًا.
- عند فتح التنبيهات الخاصة يتم إغلاق قائمة الإشعارات العامة تلقائيًا.
- عند الضغط على Escape يتم إغلاق القائمتين المفتوحتين.
- إصلاح حفظ تعديل الموظف حتى لا يتعامل النظام مع تعديل الموظف كإضافة موظف جديد.
- تثبيت `employeeId` في نموذج الموظف عند فتحه للتعديل، واستعادته قبل الحفظ إذا قامت طبقات قديمة بمسحه.
- منع ظهور رسالة `رقم الهوية مستخدم مسبقًا` عند حفظ نفس الموظف بنفس رقم الهوية.
- تحديث روابط الكاش إلى `v=169`.

ما تم تعديله في v168:

- تسريع فتح تحرير الموظف بمسار مباشر يتجاوز طبقات الفتح القديمة المتكررة.
- تظهر نافذة تحرير الموظف فور تعبئة البيانات الأساسية، ثم يتم تحميل الجداول والصور والمرفقات في الإطار التالي.
- منع طبقات النقر القديمة من فتح المسار البطيء عند الضغط على اسم الموظف أو زر التعديل.
- تحسين عرض صفحة الموظفين: يتم رسم التخطيط النشط فورًا، وتأجيل التخطيط غير المستخدم مثل البطاقات عند عرض الجدول.
- تأجيل تحميل صور المرفقات في قائمة الموظفين بعد ظهور النصوص والأزرار لتقليل التوقف المرئي.
- تحديث روابط الكاش إلى `v=168`.

ما تم تعديله في v167:

- إصلاح جدول التنبيهات الخاصة: عند إنهاء التنبيه يختفي زر `إنهاء` وزر `تأجيل` ويبقى زر `حذف` فقط.
- تحويل حذف التنبيه إلى حذف منطقي محفوظ، مع جدول جديد أسفل الصفحة باسم `التنبيهات المحذوفة`.
- جدول المحذوفات يعرض تفاصيل التنبيه، الموظف المرتبط، تاريخ الحذف، ومن قام بالحذف.
- تثبيت ترتيب أيقونة `التنبيهات الخاصة` وجرس `الإشعارات` بجوار صورة الحساب، بدون تبديل أماكن عند الضغط.
- عند ربط التنبيه بموظف يتم تسجيله داخل `ملاحظات الموظف` بمعرف ثابت، ويحدث نفس السطر عند التأجيل أو الإنهاء أو الحذف بدل إنشاء تكرارات.
- تحديث روابط الكاش إلى `v=167`.

ما تمت إضافته في v166 وما زال مدعومًا:

- إضافة صفحة جديدة باسم `التنبيهات الخاصة` أسفل `وثائق المنشأة`.
- إضافة جدول للتنبيهات الخاصة وزر `إضافة تنبيه خاص`.
- إضافة نافذة منبثقة لإنشاء التنبيه تشمل: الموضوع، النص، تاريخ الإنشاء، تاريخ التذكير، التذكير قبل، والربط بموظف اختياري.
- عند اختيار الربط بموظف تظهر قائمة الموظفين الحاليين.
- إضافة إجراءات لكل تنبيه: إنهاء، تأجيل، حذف، مع ألوان مناسبة لهوية الموقع.
- عند التأجيل يتم طلب تاريخ تذكير جديد، ويصبح هو التاريخ المعتمد بدل التاريخ القديم.
- إضافة أيقونة مستقلة أعلى الموقع للتنبيهات الخاصة بجوار جرس الإشعارات وصورة الحساب.
- قائمة الأيقونة العلوية تستخدم نفس تصميم قائمة الإشعارات الحالية وتعرض التنبيهات التي دخلت فترة التذكير فقط.
- حفظ التنبيهات الخاصة ضمن حالة الموقع السحابية باسم `privateAlertsV166`.

ملاحظات مهمة:

- تم الإبقاء على مفتاح التخزين `nawah-private-alerts-v166` وحقل الحالة السحابية `privateAlertsV166` حتى لا تضيع التنبيهات الموجودة عند استبدال الملفات.
- التنبيه يظهر في الأيقونة العلوية عندما يصبح تاريخ التذكير داخل مدة `التذكير قبل`، ويظل ظاهرًا إذا تأخر حتى يتم إنهاؤه أو تأجيله أو حذفه.
- إذا كانت المرفقات القديمة موجودة في Storage لكن لا تظهر، فالأغلب أن المشكلة كانت في قراءة جدول `attachments` أو سياسات Storage، وليس في الملف نفسه.
- يجب تشغيل `SUPABASE_ATTACHMENTS_STORAGE_SQL.sql` مرة واحدة من Supabase SQL Editor لضمان الحفظ الحقيقي والقراءة بعد تسجيل الدخول.
- تم تجنب تفكيك `app.js` إلى ملفات متعددة لأن الملف يحتوي سلسلة تصحيحات تعتمد على ترتيب التنفيذ.
- لا يتم حذف أي بيانات من المتصفح أو Supabase بمجرد استبدال الملفات.
- ملفات SQL الاختيارية التي تحذف أو تصفر بيانات مالية ما زالت اختيارية ولا تشغل إلا عند الحاجة.

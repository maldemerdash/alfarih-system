نسخة تنظيف احترافية v161

هذه النسخة مبنية فقط على النسخة المعتمدة:
نسخة_تحسين_سطر_تحديد_الموظف_في_الصلاحيات_v160(3).zip

ما تم تنظيفه:
1) إزالة ملفات التوثيق التاريخية القديمة README_FIX / README_V / README_STEP التي كانت تمثل طبقات تعديلات سابقة وليست جزءا من تشغيل الموقع.
2) إزالة ملفات SQL الاختيارية والخطرة الخاصة بإعادة الضبط أو تنظيف بيانات مالية قديمة من حزمة الرفع، حتى لا يتم تشغيلها بالخطأ.
3) إبقاء ملفات التشغيل الفعلية فقط:
   - index.html
   - app.js
   - style.css
   - sar-symbol.png
   - supabase/functions/admin-create-user/index.ts
4) تعطيل منطق تنظيف قديم داخل app.js باسم v32-clean-employees-leaves-travel-20260629 حتى لا يقوم بأي تفريغ أو استرجاع/تأثير غير مطلوب على بيانات الموظفين.
5) إضافة حارس v161 داخل app.js يجعل بيانات الموظفين القادمة من Supabase هي المرجع عند تحميل الحالة السحابية، ويمنع لقطة المتصفح القديمة من إعادة موظفين محذوفين بعد التحديث أو تسجيل الدخول.
6) تحديث رقم كسر الكاش في index.html من v160 إلى v161 لملفي app.js و style.css.

الرفع المطلوب:
ارفع الملفات التالية إلى نفس مكان الموقع الحالي واستبدل الموجودة:
- index.html
- app.js
- style.css
- sar-symbol.png

إذا كنت تستخدم Edge Function لإنشاء المستخدمين من Supabase، ارفع أيضا:
- supabase/functions/admin-create-user/index.ts

SQL:
لا يوجد SQL جديد مطلوب لهذه النسخة.
لا تشغل أي ملفات SQL قديمة خاصة بالتصفير أو FORCE أو OPTIONAL RESET إلا إذا طلبت ذلك صراحة.
